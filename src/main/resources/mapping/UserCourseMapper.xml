<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tustcs.matrix.dao.UserCourseMapper" >
  <resultMap id="BaseResultMap" type="com.tustcs.matrix.entity.UserCourse" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="user_id" property="userId" jdbcType="VARCHAR" />
    <result column="course_id" property="courseId" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, user_id, course_id
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from user_course
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByCourseId" parameterType="java.lang.Integer" >
    delete from user_course
    where course_id = #{courseId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.tustcs.matrix.entity.UserCourse" >
    insert into user_course (id, user_id,
      course_id)
    values (#{id,jdbcType=INTEGER}, #{userId,jdbcType=VARCHAR},
      #{courseId,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.tustcs.matrix.entity.UserCourse" >
    insert into user_course
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="userId != null" >
        user_id,
      </if>
      <if test="courseId != null" >
        course_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id,jdbcType=INTEGER},
      </if>
      <if test="userId != null" >
        #{userId,jdbcType=VARCHAR},
      </if>
      <if test="courseId != null" >
        #{courseId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.tustcs.matrix.entity.UserCourse" >
    update user_course
    <set >
      <if test="userId != null" >
        user_id = #{userId,jdbcType=VARCHAR},
      </if>
      <if test="courseId != null" >
        course_id = #{courseId,jdbcType=INTEGER},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.tustcs.matrix.entity.UserCourse" >
    update user_course
    set user_id = #{userId,jdbcType=VARCHAR},
      course_id = #{courseId,jdbcType=INTEGER}
    where id = #{id,jdbcType=INTEGER}
  </update>

  <!--添加列表查询-->
  <select id="listUserCourse" parameterType="com.tustcs.matrix.entity.UserCourse" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM user_course
    WHERE user_id =#{userId,jdbcType=VARCHAR}  OR
    course_id =  #{courseId,jdbcType=INTEGER}
  </select>

  <select id="selectUserCourseList" resultType="com.tustcs.matrix.entity.Course">
    select c.course_id,c.course_name,c.course_teacher,c.course_time,c.course_week,c.course_section,
    c.join_num,c.join_limit from user_course as uc inner join course as c on c.course_id=uc.course_id
     where user_id=#{user_id} and delete_flag=0
  </select>

  <select id="selectIsEnrolled" resultType="java.lang.Integer">
    select count(*) from user_course where course_id=#{courseId} and user_id=#{userId}
  </select>

  <select id="selectCourseUser" resultType="java.lang.String">
    select user_id from user_course where course_id=#{courseId}
  </select>

  <delete id="delete">
    delete from user_course
    where user_id=#{userId} and course_id=#{courseId}
  </delete>
</mapper>